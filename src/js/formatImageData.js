
export function formatImageData({ webformatURL, largeImageURL, tags, likes, views, comments, downloads }) {
   return {
      webformatURL,
      largeImageURL,
      tags,
      likes,
      views,
      comments,
      downloads
   };
}
